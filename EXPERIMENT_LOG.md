# 实验日志

> 本文件记录历史实验、失败实现、修复过程和原因。
> 即使某条记录已经失效，也不要删除，以便追溯。

---

## Channel

### 标量衰落问题

错误：

> 使用逐元素衰落。

结果：

> 分类准确率约 0.5。

修复：

> 改为每个用户一个标量 alpha。

结果：

> 分类准确率恢复正常。

---

## Path Loss

MATLAB参考代码：

```matlab
h_k = h_k / norm(h_k)
```

结果：

> 信道最终单位范数，距离相关增益被归一化掉。

因此码本方法无法利用距离维度的幅度差异。

---

## Beam-domain ZF

错误：

```text
pinv(H_reduce)
```

问题：

> 直接在256天线域进行ZF，对10用户的干扰消除能力过强。

修复：

```text
pinv(H_b @ F_sel_H)
```

LDMA：

```text
11.8 → 23.4
```

---

## NOMA beam grouping

错误：

> beam_list排序后使用 `i != k`，导致 `F_total[:, k]` 取错列。

修复后：

```text
NOMA:
2.69 → 23.8
```

---

## SDMA codebook

测试：

- 极域码本
- DFT码本

当前采用：

> DFT codebook

结果：

```text
SDMA ≈ 20.8
```

---

## NF-NOMA power allocation

论文：

> WMMSE + fmincon

当前实现：

> Equal Power

因此：

```text
Current = 23.81
Paper = 26.68
```

当前差异属于实现差异，不应为了拟合论文结果直接修改算法定义。

---

## Fig.8 baseline错误

错误实现：

> 给Baseline加入R_min penalty。

问题：

> 传统Baseline本身没有本文方法的最小速率约束。

正确行为：

> Baseline在不同R_min下保持自身性能，因此绘制水平线。

修复日期：

> 2026-09-03

修复commit：

> 8c73778 [基线] 修复Fig.8 Rmin惩罚错误，统一gamma=0.4

---

## GPT2 SNR

问题：

> 模型内部存在固定SNR noise。

错误：

> 训练/模型内部加噪声后，评估阶段再次加噪声。

结果：

> 产生双重噪声。

修复：

> 评估阶段bypass模型内部noise。

---

## CNN gamma设置

问题：

> CNN默认gamma=0.8，但论文Fig.6-9统一使用alpha_c=0.4。

修复：

> CNN gamma默认值从0.8改为0.4。

修复日期：

> 2026-09-03

修复commit：

> 8c73778 [基线] 修复Fig.8 Rmin惩罚错误，统一gamma=0.4

---

## 当前实验记录

| Date       | Experiment | Result | Status |
| ---------- | ---------- | ------ | ------ |
| 2026-09-03 | Fig.6      | 已生成   | ✅      |
| 2026-09-03 | Fig.7      | 基线水平线 | 🟡      |
| 2026-09-03 | Fig.8      | 基线水平线 | 🟡      |
| 2026-09-03 | Fig.9      | 已生成   | ✅      |
| 2026-09-03 | Table I    | 已完成   | ✅      |
| 2026-09-03 | Table II   | 已完成   | ✅      |
| 2026-09-20 | Fig.4      | 理论BF gain，ENFR≈[5.7,82.1]m | ✅ |
| 2026-09-20 | Fig.5      | GPT2 train/val loss，100 epoch | ✅ |

---

## Fig.6~9 图形审查（2026-09-20）

检查项：

- 论文原图形态
- 作者 Data.xlsx 数值
- 绝对约束：Fig.7/8 Baseline 必须水平线

结论：

- 绘图逻辑无空图/坐标错误
- Fig.7/8 Baseline 水平线 ✅
- Fig.6/9 单调性与排序 ✅
- 主要问题不是画错，而是：
  1. GPT2 与 CNN 差距过小，四图几乎叠线
  2. Fig.8 缺少多 Rmin 训练，曲线近水平
  3. 传统 baseline 数值低于论文（已知实现差异）

代码修复：

- `eval_fig7.py` / `eval_fig8.py` checkpoint 选取：优先论文默认 run，排除 `*rmin*` / `*gamma*` 目录
- 原因：旧逻辑 `'gamma' not in name` 会误选 `GPT2_rmin0.0`

### Fig.7 重跑（2026-09-20）

命令：

```text
python eval_fig7.py
python plot_results.py
```

选用 checkpoint：

- GPT2: `GPT2_09.02_12-35-10/best.bin`
- CNN: `CNN_09.02_22-04-51/6.pth`

关键结果（α_c=0.4）：

- GPT2 rate=31.9208, acc=0.9584, α_N=0.3410
- CNN rate=31.8652, acc=0.7928, α_N=0.3376
- Baseline 水平线保持不变

结论：

- 修复后的选取逻辑加载的是论文默认模型
- 数值与重跑前 JSON 一致，说明此前 Fig.7 结果本身可用
- GPT2/CNN 间隙仍仅约 0.06，属于模型性能差异，不是评估错误
- `⚠️ 未验证`：`eval_fig8_results.json` 尚未用同一逻辑重跑

---

## Fig.4 波束增益

依据：

- 论文第五节-B 与 Fig.4
- 公式 (3)(5)(6)(8)
- `main_generate_data.m` 中 `|b^H a|` 计算

实现：

```text
plot_results.py::plot_beamforming_gain
```

结果要点：

- 曲线先下降后上升，与论文一致
- Δ=0.1 门限对应增益 0.9
- 水平地面用户、俯仰角几何下 ENFR 约为 5.7 m ~ 82.1 m
- 不使用任何训练数据，纯理论计算

---

## Fig.5 训练曲线

问题：

- 旧 `training_curves.png` 图像为空/坐标异常
- GPT2 旧日志仅记录 `val_rate`，无验证损失
- 旧绘图将 `val_rate`(≈32) 与 `train_loss`(≈-32) 混在同一坐标，且 CNN/GPT2 混画，与论文 Fig.5（仅 proposed）不符

修复：

1. `plot_results.py::plot_training_curves` 只画 Proposed (GPT2)
2. 验证损失优先 `val_mu_loss`，否则回退 `-val_rate`
3. 标注最低验证损失对应 epoch
4. `hybrid_field_all.py` 训练日志新增 `val_mu_loss` 列

结果：

- 当前曲线来自论文默认参数 run `GPT2_09.02_12-35-10`
- 训练损失快速下降后收敛，形态与论文趋势一致
- `⚠️ 未验证`：尚未按论文 500 epoch 重训；绝对数值与论文 Fig.5（约 -27.x）不同

---

## GPT2/CNN 间隙过小诊断（2026-09-20）

问题：

- 论文 K=10 时 Proposed−CNN ≈ 0.51 bps/Hz
- 当前 ≈ 0.06 bps/Hz
- 分类准确率差距却与论文同量级（约 16%）

代码事实：

- C3 功率投影训练/默认评估均使用真值 `cl`
- `cl_hat` 仅用于分类损失与准确率，不进入预编码路径
- `p_hat/lamda_hat` 由网络从 `H` 直接回归

诊断实验：

- 脚本：`eval_clhat_diag.py`
- 权重不变，仅推理时 C3 改用 `cl_hat>=0.5`
- checkpoint：GPT2 `GPT2_09.02_12-35-10/best.bin`，CNN `CNN_09.02_22-04-51/6.pth`

结果（γ=α_c=0.4, test=1000）：

| C3 标签来源 | GPT2 rate | CNN rate | GPT2−CNN |
| ----------- | --------- | -------- | -------- |
| 真值 cl（官方） | 31.9208 | 31.8652 | +0.0555 |
| 预测 cl_hat | 31.9518 | 32.0746 | **−0.1228** |

解读：

- 推理改用 `cl_hat` **不会**拉开论文式间隙
- CNN rate 反而上升更多（+0.21 vs GPT2 +0.03），间隙变负
- 说明“C3 用了真值标签”**不是**间隙过小的主因
- 更可能原因：
  1. 预编码 `(p,λ)` 学习几乎不依赖分类质量
  2. 缺少论文中“分类→近场聚焦/远场转向”的定制波束路径
  3. 单位范数信道削弱距离维增益
- 错误分组甚至可能放松有效约束，使 sum-rate 更高，但不符合论文 C3 语义

结论：

- 官方评估仍保持 C3=真值 `cl`（与当前实现一致）
- **不**为了贴论文曲线去改算法定义
- 若要复现论文间隙，需要结构性改动（分类门控预编码），不是简单换标签

`⚠️`：`use_pred_cl_for_c3=True` 仅为诊断开关，默认 `False`，不影响现有训练/评估。

---

## Data_user.mat 数据体检（2026-09-20）

脚本：`output/_inspect_data.py`（临时诊断）

| 检查项 | 结果 | 论文/作者参考 |
| ------ | ---- | ------------- |
| shape | `(100000, 256)` complex | — |
| 样本数 | 10000（100000/10） | 10000 ✅ |
| 近场比例 | 39.68% | ENFR 几何，合理 |
| 远场比例 | 60.32% | — |
| ‖h‖₂ mean | **0.9988** | 作者 MATLAB：精确 1 |
| ‖h‖₂ std | **0.0514** | 作者：0 |
| ‖h‖₂ range | **0.54 ~ 1.48** | 作者：恒为 1 |
| \|norm−1\|<1e−3 | 仅 2.26% | 作者：100% |
| 近/远场范数 | 均约 0.999 | 无系统性偏差 |

结论：

1. **样本数正确**，不是 v0 那种 1000 样本问题。
2. **不是严格单位范数**。当前 `main_generate_data.m` 构造 Rician（κ=8, L=5）且显式不归一化；LoS 功率 κ/(κ+1)+NLoS 功率 1/(κ+1)=1，故 **平均范数≈1**，但有 ±5% 波动。
3. 与作者 MATLAB `h_k = h_k/norm(h_k)` **不完全一致**，但也不是含路径损耗的“乱尺度”（无数量级发散）。
4. 近/远场用户范数几乎相同 → **单位范数差异本身很难解释 GPT2/CNN 间隙仅 0.06**。
5. 间隙主因仍更可能是：分类不进 rate 路径 + 按 rate 选模型 + 双模型同构约束。

`⚠️ 未验证`：若需严格对齐作者信道，应重新生成 `Data_user.mat`（每用户 `h/norm(h)`）并重训；**当前未重训**，仅完成数据体检。

---

## 按论文总损失重评 checkpoint（2026-09-20）

脚本：`eval_paper_criterion.py`

准则：

- 官方当前：最大化 val RateCal
- 论文：最小化 `Ltot = Loss_pre + γ2·Loss_cl`，γ2=5，Loss_pre=MULoss(rmin=0.6, γ1=10)

checkpoint（论文默认 run，无 gamma/rmin 标签）：

- GPT2: `GPT2_08.31_12-13-10/best.bin`, `GPT2_09.02_12-35-10/best.bin`
- CNN: `CNN_09.02_12-35-07/{4,6}.pth`, `CNN_09.02_22-04-51/{5,6}.pth`

结果摘要：

| 模型 | 官方选择 (max rate) | 论文选择 (min Ltot) | 是否同一 ckpt |
| ---- | ------------------- | ------------------- | ------------- |
| GPT2 | 09.02/best.bin | 09.02/best.bin | ✅ 是 |
| CNN | 22-04-51/6.pth | 22-04-51/6.pth | ✅ 是 |

test 指标（两种选择相同）：

| | rate | acc | Ltot |
|--|------|-----|------|
| GPT2 | 31.9208 | 0.9584 | −31.7197 |
| CNN | 31.8652 | 0.7928 | −31.0915 |

间隙：

- 官方选择 rate 间隙：**+0.0555**
- 论文损失选择 rate 间隙：**+0.0555**（不变）
- 论文参考：+0.51

解读：

1. 现有 ckpt 上，**换论文总损失选模型不会拉开 rate 间隙**
2. CNN 各 epoch rate 几乎相同（≈31.927），Ltot 差异主要来自分类 MSE（Lcl≈0.15）
3. GPT2 的 Ltot 更好（−31.72 vs −31.09），优势在分类，**不传导到 rate**
4. GPT2 目录只保留了 rate 选出的 `best.bin`，无法在完整训练轨迹上重选；即便能重选，rate 也难凭空变出 0.5 间隙
5. **排除**“仅因选模型准则不同导致间隙过小”这一假设

结论：

- 官方评估结果可维持
- 间隙主因仍是结构性：分类不进 rate 路径 + 双模型同构约束/预编码学习
- 下一步若要贴近论文 rate 间隙，需要结构性实验（分类门控预编码）或接受差异并写明

---
